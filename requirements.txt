# Runtime dependencies — Churn Prediction API + model export
# Mantido enxuto para uma imagem Docker leve. Dependências de
# experimentação (mlflow, pandera, jupyter, matplotlib) ficam nos
# extras do pyproject.toml, não aqui.
numpy>=1.24,<2.0
pandas>=2.0,<3.0
scikit-learn>=1.3,<2.0
torch>=2.1
joblib>=1.3
fastapi>=0.110
uvicorn[standard]>=0.29
pydantic>=2.0
